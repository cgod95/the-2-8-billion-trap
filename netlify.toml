# Netlify build config. The piece is a single scroll-driven page, so no SPA
# redirect rules are needed — every URL is served by index.html.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
